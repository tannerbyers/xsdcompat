# xsdcompat

`xsdcompat` is a planned zero-third-party-dependency Go CLI and library for deterministic XSD schema-set change and compatibility analysis.

The project is in the design phase. The initial architecture, security model, compatibility semantics, licensing decisions, and release requirements will be reviewed before implementation begins.
